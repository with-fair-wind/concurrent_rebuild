# 基于 Google 风格，但将缩进宽度改为 4
BasedOnStyle: Google
IndentWidth: 4
TabWidth: 4      # 如果你希望制表符宽度也为 4，可以加上这一行
UseTab: Never    # 强制使用空格缩进（可选，默认 Google 就是不用 Tab）
# —— 以下两行控制注释不被重排/换行 —— 
ReflowComments: false   # 不自动重排注释文本